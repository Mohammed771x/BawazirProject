[pytest]
testpaths = tests
# `tests/` is on the path so conftest's fixtures can be imported by name, and
# the package root is too, so `from app import ...` resolves without an install.
pythonpath = . tests
addopts = -q
